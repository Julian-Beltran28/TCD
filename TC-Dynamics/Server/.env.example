# Variables de entorno para Railway
# Copia estas variables en la configuración de Railway

# Puerto (Railway lo asigna automáticamente)
PORT=8084

# Base de datos MySQL
DB_HOST=tu-host-de-base-de-datos
DB_USER=tu-usuario-de-db
DB_PASSWORD=tu-password-de-db
DB_NAME=tu-nombre-de-db
DB_PORT=3306

# JWT Secret (genera uno nuevo y seguro)
JWT_SECRET=tu-jwt-secret-super-seguro-aqui

# Entorno
NODE_ENV=production

# CORS (opcional, para mayor seguridad)
ALLOWED_ORIGINS=https://tu-dominio-frontend.vercel.app,https://otro-dominio.com